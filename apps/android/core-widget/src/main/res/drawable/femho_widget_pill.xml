<?xml version="1.0" encoding="utf-8"?>
<!-- La mateixa forma que el camp d'afegida ràpida de l'app, perquè en obrir-lo continuï. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/femho_input_bg" />
    <stroke android:width="1dp" android:color="@color/femho_input_border" />
    <corners android:radius="100dp" />
</shape>
