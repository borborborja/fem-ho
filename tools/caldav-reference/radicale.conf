# Radicale mínim per comparar-hi respostes. Sense autenticació i amb dades efímeres:
# és un mirall, no un servidor de veritat.
[server]
hosts = 0.0.0.0:5232

[auth]
type = none

[storage]
filesystem_folder = /data/collections

[rights]
type = authenticated
