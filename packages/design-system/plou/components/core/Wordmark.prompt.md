The Plou brand lockup. There is no logo mark — the word IS the mark, always Roboto 900.

\`\`\`jsx
<Wordmark size={24} place="Navata" />
<Wordmark tone="white" size={21} place="Navata" />  {/* over the radar map */}
\`\`\`

Gradient-clipped text uses the darker \`--gradient-brand-text\` triad so it stays legible on light surfaces. The place name sits on the same baseline at ~54% of the wordmark size, in regular weight.
