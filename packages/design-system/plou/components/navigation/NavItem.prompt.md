Desktop sidebar navigation row. The active row is the only gradient element in the sidebar.

\`\`\`jsx
<NavItem icon={<Icon name="radar" size={19} />} active>Radar</NavItem>
<NavItem icon={<Icon name="forecast" size={19} />}>Previsión</NavItem>
\`\`\`

Note the 14px radius — the sidebar nav is the one control in Plou that is rounded-rect rather than a capsule.
